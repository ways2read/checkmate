# Python
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
env/
*.egg-info/
*.egg
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Packaging output (regenerate with scripts/package.py)
dist/
build/
*.spec

# Inno Setup compiler output
installer/Output/

# Large binaries — downloaded by packaging / first-run scripts, not for git
#   runtime/   = bundled Temurin JRE
#   checker/   = bundled or locally fetched eBraille Checker
#   epubcheck/ = bundled or locally fetched W3C EPUBCheck
#   verapdf/   = bundled or locally fetched veraPDF CLI
#   ace/       = bundled Ace by DAISY (Node + Chromium)
runtime/
checker/
epubcheck/
verapdf/
ace/

# Local IDE / OS
.idea/
.vscode/
.cursor/
*.log
.DS_Store
Thumbs.db

# Accidental sample publications at repo root (testdata/ has its own ignore)
*.ebrl
*.Ebrl
*.EBRL
*.epub
*.EPUB
*.pdf
*.PDF
fido.secrets.json
